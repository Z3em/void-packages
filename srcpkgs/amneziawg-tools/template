# Template file for 'amneziawg-tools'
pkgname=amneziawg-tools
version=3.1.20260812
revision=1
build_wrksrc="src"
build_style=gnu-makefile
make_build_args="WIREGUARD_TOOLS_VERSION=${version}"
make_install_args="WITH_BASHCOMPLETION=yes WITH_WGQUICK=yes WITH_SYSTEMDUNITS=no"
depends="bash iproute2 openresolv"
checkdepends="clang-analyzer"
short_desc="Amnezia WireGuard VPN tunnel - userland tools"
maintainer="Z3em <anem@protonmail.com>"
license="GPL-2.0-only"
homepage="https://github.com/amnezia-vpn/amneziawg-tools"
distfiles="https://github.com/amnezia-vpn/amneziawg-tools/archive/refs/tags/v${version}.tar.gz"
checksum=dbd8ce0748d835d18f30bb76720246b7bfc80bd09cd17c379b1c59f683a18493
make_dirs="/etc/amnezia/amneziawg 0700 root root"

post_patch() {
	vsed -i 's/^CFLAGS /override &/' Makefile
}

post_install() {
	vsv amneziawg
}
